pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git "https://github.com/arish-akhtar/nodejs-app.git"
            }
        }

        stage("terraform init") {
            steps {
                sh ('terraform init');
            }
        }
        
        stage("terraform plan") {
            steps {
                sh ('terraform plan');
            }
        }
                
        stage("Terraform Action") {
    steps {
        echo "Executing Terraform action"
        sh ('terraform apply --auto-approve');
            }
        }
    }
    post {
        success {
            script {
                echo 'Deployment successful!'
            }
        }
    }
}
